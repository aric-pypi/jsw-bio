import jsw_bio as bio
import jsw_nx as nx


def test_download_accids():
    pass
    # the 28 records
    # res1 = bio.ncbi_download_accids(term='cas12')

    # not exist
    # bio.entrez_download_accids(term='cas15')

    # assert len(res1) > 0
    # assert len(res2) == 0
